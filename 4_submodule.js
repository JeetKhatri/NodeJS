//module is file name and run that file

module.exports.sum = function(a,b) {
    return `The sum of ${a} and ${b} is ${a+b}`;
}

module.exports.arr_cnt = function(arr) {
    return 'The length of array is '+arr.length;
}

module.exports.pi = 3.14;
